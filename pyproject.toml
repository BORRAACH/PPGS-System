[project]
name = "pizzeria-system"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "PyQt6",
    "certifi",
    "cryptography",
    "pywin32; sys_platform == 'win32'",
    "qtawesome",
    "segno",
    "zeroconf",
]
